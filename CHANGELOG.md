# Changelog — OmniType-FreePTT

همه تغییرات مهم این پروژه به ترتیب نسخه ثبت می‌شود.
قالب: [Keep a Changelog](https://keepachangelog.com/fa-IR/1.1.0/) — نسخه‌بندی: [SemVer](https://semver.org/lang/fa/).## [Unreleased]

### Added
- **تشخیص خودکار سکوت (VAD):** ضبط خودکار پس از قطع صدا متوقف می‌شود — فعال‌سازی از تب تنظیمات پنل کنترل با قابلیت تنظیم زمان سکوت (پیش‌فرض ۱٫۵ ثانیه).

### Tests
- `tests/test_vad.py` — ۹ تست برای VAD tracker و تنظیمات

 — به‌روزرسانی خودکار، اکسپورت/ایمپورت و متن‌به‌گفتار

### ✨ امکانات جدید
- **🔊 متن‌به‌گفتار نتیجهٔ ترجمه** (`core/tts.py`, `gui/document_translator.py`):
  دکمه «🔊 پخش صدا» در پنجرهٔ ترجمهٔ اسناد، نتیجهٔ ترجمه را با صدای سیستم (pyttsx3/SAPI5) می‌خواند. صدای مناسب بر اساس زبان متن انتخاب می‌شود (فارسی → صدای fa اگر موجود، انگلیسی → صدای en) و پخش در thread جداگانه انجام می‌شود تا پنجره قفل نشود. اگر pyttsx3 نصب نباشد یا خطایی رخ دهد، بی‌صدا نادیده گرفته می‌شود.
- **۷ تست برای `core/tts`** (`tests/test_tts.py`): تشخیص زبان فارسی، انتخاب صدا (فارسی/انگلیسی/پیش‌فرض/فیلد languages)، عدم پخش برای ورودی خالی، و رفتار بی‌خطا بدون pyttsx3.
- **📤/📥 اکسپورت/ایمپورت تنظیمات** (`core/export_import.py`):
  خروجی تمام تنظیمات (.env)، موتورها و واژه‌نامه به فایل JSON و بازیابی آن با فراخوانی `gather_export_data()` و `apply_import_data()` — همه بدون تماس با دیسک از حافظهٔ زندهٔ برنامه. در تب تنظیمات پنل کنترل دو دکمه «📤 خروجی» و «📥 ورودی» با دیالوگ فایل و تأیید قبل از بازنویسی.
- **۶ تست برای `core/export_import`** (`tests/test_export_import.py`): ساختار خروجی، حذف ENGINES از settings، فرمت نامعتبر، اعمال ایمپورت روی حافظه، و صحت نوشتن واژه‌نامه به فایل.
- **🔄 چک‌کنندهٔ به‌روزرسانی خودکار** (`core/updater.py`):
  نسخهٔ محلی را از `version_info.txt` می‌خواند و آخرین ریلیز گیت‌هاب (`mahdimoslemi88-sys/OmniType-FreePTT`) را چک می‌کند؛ اگر نسخهٔ جدیدتری باشد در tooltip تسکبار و بخش به‌روزرسانی پنل کنترل اعلام می‌شود. آیتم منوی تسکبار «🔄 بررسی به‌روزرسانی» + دکمهٔ همان در تب تنظیمات پنل کنترل نتیجه را با دیالوگ نشان می‌دهد (لینک ریلیز و فایل zip). هیچ خطای شبکه‌ای برنامه را نمی‌شکند (باگ‌آزاد).
- **۶ تست برای `core/updater`** (`tests/test_updater.py`): پارس برچسب، خواندن نسخهٔ محلی، یافتن/نداشتن نسخهٔ جدید، خطای غیر-200 و قطعی شبکه با API گیت‌هاب mockشده.
- **🎙️ انتخاب میکروفون و نشانگر سطح صدا زنده** (`core/audio.py`, `gui/app.py`, `gui/control_panel.py`):
  بخش «میکروفون» در تب تنظیمات پنل کنترل — کشویی دستگاه‌های ورودی (شمارش با pyaudio)، ذخیرهٔ انتخاب در `.env` (`INPUT_DEVICE_INDEX`) و اعمال آن هنگام باز کردن استریم ضبط. یک نشانگر سطح صدا (۰..۱ بر اساس RMS) هنگام ضبط به‌صورت زنده به‌روزرسانی می‌شود (سبز/زرد/قرمز) و پس از پایان ضبط صفر می‌شود. تابع `get_input_level` و `list_input_devices` به `core/audio.py` اضافه شد.
- **۷ تست برای صدا/میکروفون** (`tests/test_audio.py`, `tests/test_control_panel.py`): سطح سکوت/رشد/کمپ‌شدن RMS، پارس ایندکس میکروفون، ساخت بخش میکروفون بدون دستگاه، و به‌روزرسانی امن نشانگر.

---

## [2.2.0] — 2026-08-24

نسخه ۲.۲: رفع باگ‌های مسیریابی زبان در تبدیل گفتار به متن (ASR) و بهبود گزارش خطا در مهندسی پرامپت.

### 🐛 رفع باگ‌ها (Fixed)

- **ASR حالت «مهندسی پرامپت» گفتار فارسی را با انگلیسی تشخیص می‌داد** (`engine/asr.py`):
  حالت `prompt_engineer` (که گفتار فارسی را به پرامپت مهندسی‌شده تبدیل می‌کند) در نقشه زبان گوگل نبود، بنابراین با `en-US` روی گفتار فارسی اجرا می‌شد و شکست می‌خورد. حالا `prompt_engineer` به `fa-IR` نگاشت می‌شود. به‌علاوه، منطق نگاشت به یک هِلپر قابل تست (`_google_lang`) استخراج شد تا مسیر گوگل و مسیر بازگشت موتور سفارشی هر دو یک رفتار صحیح داشته باشند.
- **خطای مهندسی پرامپت در exe بی‌صدا بود** (`gui/app.py`):
  اگر موتور LLM پیکربندی/معتبری وجود نداشته باشد، ساخت پرامپت خطا می‌داد که فقط در کنسول چاپ می‌شد — در exe ویندوز کنسول دیده نمی‌شود و کاربر هیچ بازخوردی نمی‌گرفت. حالا خطا روی پنجرهٔ اصلی برنامه با یک پیام واضح (`showerror`) به کاربر نمایش داده می‌شود.
- **تبدیل تلفظ حروف، واژه‌های واقعی فارسی را خراب می‌کرد** (`core/normalizer.py`):
  تلفظ حرف‌هایی که هم‌نویس با واژه/ساختار رایج فارسی‌اند (بی=بدون، وی=او، او، ان=آن، ام، ار، سی=۳۰، دی=دیروز، ای، آی) به‌تنهایی به حرف انگلیسی تبدیل می‌شدند — مثلاً «من بی صبرم» به «من B صبرم» و «وی گفت» به «V گفت». حالا این واژه‌ها حفظ می‌شوند؛ فقط وقتی متن به‌وضوح تلفظِ رشته حروف باشد (چند نام حرف پشت‌سرهم مثل «بی سی دی اف») تبدیل می‌شوند.
- **کامأ فارسی «،» در نرمال‌ساز علائم نبود** (`core/normalizer.py`):
  در `fix_punctuations` کلاس علائم نگارشی فقط کامأ ASCII (`,`) را داشت و کامأ فارسی U+060C را نه — برای همین متن‌هایی مثل «سلام ،دنیا» هیچ‌وقت درست فاصله‌گذاری نمی‌شدند. حالا «،» هم به کلاس اضافه شده است.
- **کرش منوی تسکبار با callback ثبت‌نشده** (`gui/system_tray.py`):
  منو از `self._callbacks.get(name, noop)()` استفاده می‌کرد؛ چون کلیدها با مقدار `None` از قبل وجود دارند، `.get` در آن حالت `None` را برمی‌گرداند و کلیک روی آن آیتم کرش می‌کرد. متد امن `_fire(name)` اضافه شد که callback صفر را نادیده می‌گیرد.

### 🧪 تست‌ها (Tests)
- **۴ تست جدید برای نگاشت زبان Google Speech** (`tests/test_asr_routing.py`):
  اطمینان از اینکه حالت‌های فارسی (شامل `prompt_engineer`) به `fa-IR` و حالت‌های انگلیسی به `en-US` می‌روند.
- **۱۲ تست جدید normalizer** (`tests/test_normalizer.py`):
  تلفظ حروف (رشته‌ها و حروف تک‌مطمئن)، حفظ واژه‌های هم‌نویسِ رایج فارسی، نرمال‌سازی حروف عربی/نیم‌فاصله/پرکننده/علائم و رگرسیون کامأ فارسی.
- **۲ تست لبهٔ چانک‌بندی translator** (`tests/test_translator.py`): کلمهٔ تکی خیلی بلند (>۴۵۰۰) بدون حلقهٔ بی‌پایان و ورودی خالی.
- **گسترش تست‌های translator** (`tests/test_translator.py`): اطمینان از اینکه هیچ چانک از حد ۴۵۰۰ رد نشود، ترجیح برش در مرز نوبلاین، و ادامهٔ زنجیره وقتی deep-translator بدون خطا نتیجهٔ خالی بدهد.
- **۹ تست GUI برای پنل کنترل** (`tests/test_control_panel.py`): وجود ۶ تب، دکمه‌های موتور (گوگل/محلی)، گوگل پیش‌فرض و فعال (سبز)، انتخاب موتور/زبان و بازتاب روی parent، نشانگر وضعیت سبز برای گوگل بدون تست شبکه، و بستن امن. (با یک ریشهٔ مشترک Tk؛ اگر Tk در محیط نباشد تست‌ها skip می‌شوند.)
- **۸ تست مهندسی پرامپت** (`tests/test_prompt_engineer.py`): ورودی خالی، اولویت LLM، ردِ کاندید بدون کلید، fallback مدل دوم، سقوط به Gemini، و خطای RuntimeError بدون LLM.
- **۱۰ تست تسکبار** (`tests/test_system_tray.py`): تولید تصویر آیکون، ثبت/اجرای callback، ساخت منو، و رفتار بی‌خطر بدون آیکون/pystray (با رفع کرش `_fire`).
- **تست‌های میانبر و کنترل رسانه** (`tests/test_hotkey.py`, `tests/test_media_control.py`): نگاشت VK و نرمال‌سازی ترکیب کلیدها با `is_down` مهارشده، و منطق توقف/ادامهٔ رسانه (غیرفعال، toggle، بدون pause قبلی، reset) با `SendInput` مهارشده.
- **تست‌های صدا و واژه‌نامه** (`tests/test_audio.py`, `tests/test_dictionary_window.py`): تبدیل معتبر PCM→WAV (هدر/پارامتر/round-trip/خالی/پارامتر سفارشی) و ساخت/افزودن/حذف در پنجرهٔ واژه‌نامه با یک ریشهٔ مشترک Tk و مدیر stub.

---

## [2.1.0] — 2026-08-24

نسخه ۲.۱: بازسازی کامل به ساختار چندپوشه‌ای، توقف خودکار ویدیو/موزیک، آیکون تسکبار، سیستم چندموتوره و پنل کنترل تب‌دار.

### 🏗️ ساختار پروژه (Refactor)
- فایل تکی ۲۰۸۷ خطی `OmniType-FreePTT.py` به یک **پروژه ماژولار** تبدیل شد:
  - `core/` — تنظیمات (.env)، واژه‌نامه، صدا، میانبر، کنترل رسانه، مسیرها، نرمال‌ساز
  - `engine/` — ترجمه، مهندسی پرامپت، تبدیل گفتار به متن (ASR)، موتور محلی
  - `gui/` — کلاس اصلی اپ، پنل کنترل، تسکبار و همه پنجره‌ها
- `OmniType-FreePTT.py` حالا فقط یک **لانچر نازک** است.
- `run.bat` برای اجرای تستی از سورس (با کنسول برای دیدن خطاها).
- `requirements.txt` برای نصب وابستگی‌ها.

### ✨ امکانات جدید (Added)
- **⏸️ توقف خودکار ویدیو/موزیک هنگام صحبت** (`core/media_control.py`):
  با شروع ضبط، کلید Media Play/Pause ویندوز ارسال می‌شود و با پایان ضبط، رسانه ادامه می‌یابد — روی یوتیوب، اسپاتیفای، VLC و مرورگرها کار می‌کند. فقط اگر خود برنامه رسانه را متوقف کرده باشد، دوباره پخش می‌کند. (قابل خاموش/روشن از پنل کنترل و منوی تسکبار، ذخیره در `.env`)
- **🖥️ آیکون در ناحیه هیدن‌آیکون‌های تسکبار** (`gui/system_tray.py`):
  آیکون واقعی برنامه + منوی کامل (واژه‌نامه، تنظیمات موتورها، توقف خودکار ویدیو، آزادسازی VRAM، خروج) + tooltip زنده با وضعیت (آماده / در حال ضبط / در حال پردازش).
- **🎛️ پنل کنترل کامل به‌جای منوی راست‌کلیک** (`gui/control_panel.py`):
  پنجره تب‌دار و اسکرول‌پذیر با ۶ تب: عملیات سریع، موتورها، زبان، میانبر، تاریخچه، تنظیمات.
- **🤖 سیستم چندموتوره** (`core/config.py`):
  ذخیره چند موتور/مدل در `.env` (کلید `ENGINES` به صورت JSON)؛ هر موتور یک نقش دارد (فقط ASR / فقط LLM / هر دو) و **ترتیب لیست = اولویت استفاده**؛ دکمه‌های بالا/پایین برای تغییر اولویت.
- **✏️ فرم ساده افزودن پرووایدر** (`gui/api_settings_window.py`):
  فقط ۴ فیلد: نام، Base URL، کلید و نام دقیق مدل + نقش + پریست‌های آماده (Groq / OpenAI / OpenRouter / محلی).
- **🧪 تست اتصال همان‌لحظه‌ای** (`core/engine_test.py`):
  دکمه «تست اتصال و مدل» در فرم افزودن پرووایدر (بدون نیاز به ذخیره) + دکمه «تست همه موتورها» در پنل کنترل با نمایش نتیجه هر موتور در لیست.
- **🟢 نشانگر زنده وضعیت موتور** در بالای پنل کنترل: نقطه رنگی سبز/قرمز/زرد + بررسی دوره‌ای هر ۶۰ ثانیه.
- **🔤 تبدیل تلفظ فارسی حروف انگلیسی** (در نرمال‌ساز): «پی» → `P`، «بی سی دی اف» → `BCDF`.

### 🐛 رفع باگ‌ها (Fixed)
- **واژه‌نامه: پنجره هنگام باز شدن کرش می‌کرد** — فونت `("Segoe UI", 9.5)` که Tk نمی‌پذیرفت با فونت صحیح جایگزین شد؛ ورود کلمه حالا کار می‌کند (افزودن/حذف تست شده).
- **مسیر فایل‌ها در حالت exe**: `.env` و `custom_dictionary.json` حالا frozen-aware هستند و کنار فایل اجرایی خوانده/نوشته می‌شوند (`core/paths.py`).
- **ترجمه متون چندخطی**: قطعه‌بندی خراب که خروجی تکراری می‌داد اصلاح شد.
- **ترجمه «متن را کپی می‌کرد به‌جای ترجمه»**: Google Translate Free گاهی 429 می‌دهد؛ موتور `deep-translator` به‌عنوان موتور اول اضافه شد (کیفیت بالاتر و پایدارتر) و LLM/Gemini فقط fallback هستند.
- **مهندسی پرامپت (Ctrl+Alt+P) فقط ترجمه می‌کرد**: کلید API فقط برای ASR ذخیره شده بود و مدل پیش‌فرض `llama-3.3-70b-versatile` از Groq حذف شده بود (404)؛ کلید به LLM هم اعمال و مدل پیش‌فرض به `openai/gpt-oss-20b` تغییر کرد. حالا به‌جای سقوط بی‌صدا، خطای واضح نشان داده می‌شود.
- **`get_selected_text` و `DocumentTranslatorWindow` تعریف نشده بودند**: پرامپت تخصصی و ترجمه اسناد کرش می‌کردند — هر دو پیاده‌سازی شدند.
- **اسکرول پنجره مدیریت موتورها**: انتهای محتوا دیده نمی‌شد — `scrollregion` حالا با تغییر اندازه محتوای داخلی هم به‌روز می‌شود.
- **پنجره‌ها همیشه روی صفحه می‌ماندند**: `-topmost` از پنجره‌های تعاملی (پنل کنترل، تنظیمات، واژه‌نامه، میانبر) حذف شد؛ فقط گوی شناور و پاپ‌آپ ترجمه روی صفحه می‌مانند.
- **خروج تمیز برنامه**: توقف رسانه، بستن تسکبار، unhook کیبورد و بستن صدا.

### 🔧 تغییرات دیگر (Changed)
- موتور پیش‌فرض تبدیل صوت به متن: **Google Speech رایگان** (بدون نیاز به کلید) — با انتخاب یک‌کلیکی از تب موتورها قابل تغییر است.
- ترتیب موتورهای ترجمه: **Google (دقیق) اول**، سپس LLM سفارشی، سپس Gemini — چون ترجمه مدل‌های LLM کیفیت پایین‌تری داشت.
- نسخه متادیتای exe به `2.1.0` ارتقا یافت.

---

## [2.0.0] — 2026-08-22

### ✨ امکانات جدید (Added)
- **🪄 مهندسی پرامپت (Prompt Engineering)**: تبدیل درخواست گفتاری/متنی به پرامپت ساختاریافته AI با میانبر Ctrl+Alt+P.
- **✨ پاپ‌آپ شناور مشاهده ترجمه (Smart Highlight Peek Popup)**: ترجمه متن انتخاب‌شده در محل نشانگر موس با دکمه‌های کپی و جایگزینی (Ctrl+Alt+X).
- **🪟 متادیتای نسخه ویندوز (VersionInfo)** برای فایل exe.

---

## [2.0] — 2026-08-17

### ✨ امکانات جدید (Added)
- **🌐 سرویس‌های هوش مصنوعی چندگانه**: پشتیبانی از ارائه‌دهندگان دلخواه (سازگار با OpenAI) برای تبدیل گفتار و ترجمه.
- **⌨️ کلید میانبر دلخواه**: انتخاب PTT از میان گزینه‌های آماده یا ضبط کلید دلخواه.
- **🇮🇷 نرمال‌ساز متون فارسی**: اصلاح حروف عربی، نیم‌فاصله‌ها، علائم و کلمات پرکننده.
- **🏗️ ساختار چندفایلی**: پایه‌ریزی ماژولار پروژه.

---

## [1.0] — اولین انتشار

- **🎙️ تایپ صوتی Push-to-Talk**: فارسی/انگلیسی/خودکار/ترجمه همزمان.
- **📚 واژه‌نامه تخصصی** اصطلاحات فنی فارسی↔انگلیسی.
- **🖥️ موتور محلی Faster-Whisper** برای تبدیل گفتار آفلاین.
- **🟢 گوی شناور** با انیمیشن‌های زنده وضعیت.
